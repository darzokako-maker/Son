name: SonOyuncu-Blink-Build

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    # Derleyici araçlarını (cl.exe) tanımlayan adım
    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1

    # WinDivert dosyalarını indirip klasöre çıkaran adım
    - name: Download WinDivert
      run: |
        curl -L https://github.com/basil00/WinDivert/releases/download/v2.2.2/WinDivert-2.2.2-A.zip -o windivert.zip
        expand-archive windivert.zip -destinationpath wd

    # Hatanın düzeltildiği derleme komutu
    - name: Compile
      run: |
        cl.exe /EHsc /I wd/WinDivert-2.2.2-A/include main.cpp /link /LIBPATH:wd/WinDivert-2.2.2-A/x64 WinDivert.lib user32.lib /OUT:SonOyuncuBlink.exe

    # EXE ve gerekli sürücüleri paketleyen adım
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: SonOyuncu-Blink-EXE
        path: |
          SonOyuncuBlink.exe
          wd/WinDivert-2.2.2-A/x64/WinDivert64.sys
          wd/WinDivert-2.2.2-A/x64/WinDivert.dll
          
